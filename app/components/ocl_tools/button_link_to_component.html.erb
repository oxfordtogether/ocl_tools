<% case type %>
  <% when "primary-grey" %>
    <%= link_to path, class: "inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-gray-600 hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-500" do %>
      <% if icon_name %>
        <%= icon(icon_name, class: "-ml-1 mr-2 h-5 w-5 text-gray-500") %>
      <% end %>
      <%= text %>
    <% end %>
  <% when "secondary-grey" %>
    <%= link_to path, class: "inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-gray-700 bg-gray-200 hover:bg-gray-300 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-500" do %>
      <% if icon_name %>
        <%= icon(icon_name, class: "-ml-1 mr-2 h-5 w-5 text-gray-500") %>
      <% end %>
      <%= text %>
    <% end %>
    <% when "primary-green" %>
    <%= link_to path, class: "inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-green-600 hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-500" do %>
      <% if icon_name %>
        <%= icon(icon_name, class: "-ml-1 mr-2 h-5 w-5 text-green-500") %>
      <% end %>
      <%= text %>
    <% end %>
  <% when "secondary-green" %>
    <%= link_to path, class: "inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-green-700 bg-green-200 hover:bg-green-300 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-500" do %>
      <% if icon_name %>
        <%= icon(icon_name, class: "-ml-1 mr-2 h-5 w-5 text-green-500") %>
      <% end %>
      <%= text %>
    <% end %>
  <% when "primary-blue" %>
    <%= link_to path, class: "inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" do %>
      <% if icon_name %>
        <%= icon(icon_name, class: "-ml-1 mr-2 h-5 w-5 text-blue-500") %>
      <% end %>
      <%= text %>
    <% end %>
  <% when "secondary-blue" %>
    <%= link_to path, class: "inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-blue-700 bg-blue-200 hover:bg-blue-300 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" do %>
      <% if icon_name %>
        <%= icon(icon_name, class: "-ml-1 mr-2 h-5 w-5 text-blue-500") %>
      <% end %>
      <%= text %>
    <% end %>
  <% when "white" %>
    <%= link_to path, class: "inline-flex items-center px-4 py-2 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-500" do %>
      <% if icon_name %>
        <%= icon(icon_name, class: "-ml-1 mr-2 h-5 w-5 text-gray-500") %>
      <% end %>
      <%= text %>
    <% end %>
  <% when "warning-primary" %>
    <%= link_to path, class: "inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-red-600 hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500" do %>
      <% if icon_name %>
        <%= icon(icon_name, class: "-ml-1 mr-2 h-5 w-5 text-gray-500") %>
      <% end %>
      <%= text %>
    <% end %>
  <% when "warning-secondary" %>
    <%= link_to path, class: "inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-red-700 bg-red-100 hover:bg-red-200 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500" do %>
      <% if icon_name %>
        <%= icon(icon_name, class: "-ml-1 mr-2 h-5 w-5 text-gray-500") %>
      <% end %>
      <%= text %>
    <% end %>
  <% else %>
    <%= link_to path, class: "inline-flex items-center px-4 py-2 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" do %>
      <% if icon_name %>
        <%= icon(icon_name, class: "-ml-1 mr-2 h-5 w-5 text-gray-500") %>
      <% end %>
      <%= text %>
    <% end %>
<% end %>


