<%= link_to path, **options, class: "#{style.link_classes} inline-flex items-center px-4 py-2 border rounded-md shadow-sm text-sm font-medium focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" do %>
  <% if icon_name %>
    <%= icon(icon_name, class: "-ml-1 mr-2 h-5 w-5 #{style.icon_classes}") %>
  <% end %>
  <%= text %>
<% end %>
