<%= link_to path, **options, class: "#{style.link_classes} inline-flex items-center #{size.link_classes} border rounded-md shadow-sm text-sm font-medium focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" do %>
  <% if icon_name %>
    <%= icon(icon_name, class: "#{size.icon_classes} #{style.icon_classes}") %>
  <% end %>
  <%= text %>
<% end %>
