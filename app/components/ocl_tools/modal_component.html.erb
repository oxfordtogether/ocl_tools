<div class="fixed inset-0 z-30 bg-gray-500 bg-opacity-75 transition-opacity" aria-hidden="true"></div>
<div class="fixed inset-0 z-30 overflow-y-auto flex items-center justify-center" >
  <div class="max-h-screen w-full <%= modal_width %> relative">
    <div class="p-8 m-1 bg-white rounded shadow">
      <% if title %>
        <h1 class="text-center mb-4 font-semibold tracking-wide text-lg text-gray-600">
          <%= title %>
        </h1>
      <% end %>
      <div class="mt-2">
        <%= body %>
      </div>
    </div>
  </div>
</div>
