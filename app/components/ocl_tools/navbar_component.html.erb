  <nav data-controller='visibility-toggle' class="bg-primary-600">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-16">
      <div class="flex items-center">
        <div class="flex-shrink-0 w-20 text-center leading-5 text-primary-200">
          <%= link_to brand_name, home_path %>
        </div>
        <div class="hidden md:block">
          <div class="ml-10 flex items-baseline space-x-4">
            <% links.each do |link| %>
              <% if link.active? %>
                <%= link_to link.name, link.path, class: "bg-primary-700 text-white px-3 py-2 rounded-md text-sm font-medium tracking-wide text-center leading-4" %>
              <% else %>
                <%= link_to link.name, link.path, class: "text-white hover:bg-primary-500 hover:bg-opacity-75 px-3 py-2 rounded-md text-sm font-medium tracking-wide text-center leading-4" %>
              <% end %>
            <% end %>
          </div>
        </div>
      </div>
      <div class="hidden md:block">
        <div class="ml-4 flex items-center md:ml-6">
          <% right_links.each do |link| %>
            <% if link.icon %>
              <div>
                <%= link_to link.path, class: "flex bg-primary-600 p-1 rounded-full text-primary-200 hover:text-white" do %>
                  <%= icon(link.icon, class: "h-6 w-6") %>
                  <span class="ml-2"><%= link.name %></span>
                <% end %>
              </div>
            <% else %>
              <%= link_to link.name, link.path, class: "ml-5 flex-shrink-0 text-center leading-5 text-primary-200 hover:text-white"%>
            <% end %>
          <% end %>
          <!-- Profile dropdown -->
          <div class="ml-8 relative" data-controller='visibility-toggle'>
            <div>
              <button data-action="click->visibility-toggle#toggle" class="relative z-10 max-w-xs bg-primary-600 rounded-full flex items-center text-sm text-white focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-offset-primary-600 focus:ring-white" id="user-menu" aria-haspopup="true">
                <span class="sr-only">Open user menu</span>
                <%= icon("person-circle", class: "h-8 w-8") %>
              </button>
            </div>
            <!--
                Profile dropdown panel
              -->
            <button data-action="click->visibility-toggle#close" tabindex="-1" class="hidden fixed inset-0 h-full w-full bg-black opacity-50 cursor-default"
              data-visibility-toggle-target='becomesVisible'
              data-transition-enter="transition ease-out duration-100"
              data-transition-enter-start="opacity-0"
              data-transition-enter-end="opacity-50"
              data-transition-leave="transition ease-in duration-75"
              data-transition-leave-start="opacity-50"
              data-transition-leave-end="opacity-0"
              ></button>
            <div class="hidden origin-top-right absolute right-0 mt-2 rounded-md shadow-lg py-1 bg-white ring-1 ring-black ring-opacity-5" role="menu" aria-orientation="vertical" aria-labelledby="user-menu"
                  data-visibility-toggle-target='becomesVisible'
                  data-transition-enter="transition ease-out duration-100"
                  data-transition-enter-start="transform opacity-0 scale-95"
                  data-transition-enter-end="transform opacity-100 scale-100"
                  data-transition-leave="transition ease-in duration-75"
                  data-transition-leave-start="transform opacity-100 scale-100"
                  data-transition-leave-end="transform opacity-0 scale-95"
              >
              <span class="block px-4 py-2 text-sm text-gray-400" role="menuitem"><%= email %></span>
              <% profile_links.each do |link| %>
                <%= link_to link.name, link.path, class: "block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" %>
              <% end %>
            </div>
          </div>
        </div>
      </div>
      <div class="-mr-2 flex md:hidden">
        <!-- Mobile menu button -->
        <button data-action="click->visibility-toggle#toggle" class="bg-primary-600 inline-flex items-center justify-center p-2 rounded-md text-primary-200 hover:text-white hover:bg-primary-500 hover:bg-opacity-75 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-offset-primary-600 focus:ring-white">
          <span class="sr-only">Open main menu</span>
          <div data-visibility-toggle-target='becomesInvisible'>
            <%= icon("menu", class: "h-6 w-6") %>
          </div>
          <div data-visibility-toggle-target='becomesVisible' class="hidden">
            <%= icon("cross", class: "h-6 w-6") %>
          </div>
        </button>
      </div>
    </div>
  </div>
  <!-- Mobile menu -->
  <div data-visibility-toggle-target='becomesVisible' class="hidden md:hidden">
    <div class="px-2 pt-2 pb-3 space-y-1 sm:px-3">
      <% links.each do |link| %>
        <% if link.active? %>
          <%= link_to link.name, link.path, class: "bg-primary-700 text-white block px-3 py-2 rounded-md text-base font-medium" %>
        <% else %>
          <%= link_to link.name, link.path, class: "text-white hover:bg-primary-500 hover:bg-opacity-75 block px-3 py-2 rounded-md text-base font-medium" %>
        <% end %>
      <% end %>
    </div>
    <div class="px-2 pt-4 pb-3 space-y-2 sm:px-3 border-t border-primary-700">
      <% right_links.each do |link| %>
        <% if link.icon %>
          <%= link_to link.path, class: "flex text-white hover:bg-primary-500 hover:bg-opacity-75 px-3 py-2 rounded-md text-base font-medium" do %>
            <%= icon(link.icon, class: "h-6 w-6") %>
            <span class="ml-2"><%= link.name %></span>
          <% end %>
        <% else %>
          <%= link_to link.name, link.path, class: "text-white hover:bg-primary-500 hover:bg-opacity-75 block px-3 py-2 rounded-md text-base font-medium"%>
        <% end %>
      <% end %>
    </div>
    <div class="pt-4 pb-3 border-t border-primary-700">
      <div class="flex items-center px-5">
        <div class="flex-shrink-0 text-white">
          <%= icon("person-circle", class: "h-10 w-10") %>
        </div>
        <div class="ml-3">
          <div class="text-base font-medium text-white"><%= email %></div>
          <div class="text-sm font-medium text-primary-300"><%= email %></div>
        </div>
      </div>
      <div class="mt-3 px-2 space-y-1">
        <% profile_links.each do |link| %>
          <%= link_to link.name, link.path, class: "block px-3 py-2 rounded-md text-base font-medium text-white hover:bg-primary-500 hover:bg-opacity-75" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
