<div class="sm:flex sm:items-center sm:justify-between">
  <div class="flex-1 min-w-0">
    <div>
      <h1 class="flex items-baseline text-2xl font-bold text-gray-900 gap-x-2">
        <%= title_section %>
      </h1>
    </div>
    <div class="mt-1 flex flex-col sm:flex-row sm:flex-wrap sm:mt-0">
      <%= subtitle_section %>
    </div>
  </div>
  <% if actions_section %>
    <div class="flex flex-col-reverse justify-stretch space-y-4 space-y-reverse sm:flex-row-reverse sm:justify-end sm:space-x-reverse sm:space-y-0 sm:space-x-3 md:mt-0 md:flex-row md:space-x-3">
      <%= actions_section %>
    </div>
  <% end %>
</div>
<%= body_section%>
<% if nav_tabs_section %>
  <%= nav_tabs_section %>
<% else %>
  <div class="mb-6"></div>
<% end %>
