Description:
    Generates an index page with filters by status

Example:
    rails generate ocl_tools:index_view_by_status admin/big --namespace admin --section people

    This will create:
        app/views/admin/bigs/index.html.erb
        app/controllers/admin/bigs_controller.rb
