Description:
    Adds search cache

Example:
    rails generate ocl_tools:search search

Requires the following to be set up:

Routes and controller, e.g.
```
# config/route.rb
get "search", to: "pages#search"

# pages_controller.rb
def search
    @status ||= :start
    @results ||= []
end
```

Seed cache
```
# seed.rb
SearchCache.refresh
```

Debounced package
```
yarn add debounced

# app/javascript/packs/application.js
import debounced from "debounced";
debounced.initialize();
```
